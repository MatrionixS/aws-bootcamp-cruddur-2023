#! /usr/bin/bash


CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL='db drop'
printf "${CYAN}==${LABEL}==${NOCOLOR}\n"

NO_DB_URL=$(sed 's/\/cruddur//g' <<< "$CONNECTION_URL")

psql $NO_DB_URL -c "DROP DATABASE cruddur;"
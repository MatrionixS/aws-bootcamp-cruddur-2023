#! /usr/bin/bash


CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL='db seed load'
printf "${CYAN}==${LABEL}==${NOCOLOR}\n"

SCHEMA_PATH=$(realpath .)/db/seed.sql

psql $CONNECTION_URL cruddur < $SCHEMA_PATH